switch:
  # Emergency mode
  - platform: template
    id: emergency_mode
    name: "Emergency Mode"
    internal: true
    optimistic: true

  - platform: template
    id: force_heat
    name: "Emergency force Heating"
    internal: true
    optimistic: true

  - platform: template
    id: force_dhw
    name: "Emergency force DHW"
    internal: true
    optimistic: true