number:
  # Equitherm
  - platform: template
    name: "Equitherm N (Slope)"
    id: eq_n
    min_value: 0.1
    max_value: 1.5
    step: 0.01
    mode: box 
    entity_category: config
    restore_value: true
    optimistic: true
    unit_of_measurement: ""
    icon: mdi:alpha-n
    web_server:
      sorting_group_id: sorting_equitherm

  - platform: template
    name: "Equitherm K (Offset)"
    id: eq_k
    min_value: 0.0
    max_value: 10.0
    step: 0.01
    mode: box 
    entity_category: config
    restore_value: true
    optimistic: true
    unit_of_measurement: ""
    icon: mdi:alpha-k
    web_server:
      sorting_group_id: sorting_equitherm

  - platform: template
    name: "Equitherm T (Base)"
    id: eq_t
    min_value: -20.0
    max_value: 20.0
    step: 0.01
    mode: box 
    entity_category: config
    restore_value: true
    optimistic: true
    device_class: temperature
    icon: mdi:alpha-t
    web_server:
      sorting_group_id: sorting_equitherm

  - platform: template
    name: "Equitherm Feedback Gain"
    id: eq_fb_gain
    min_value: -10.0
    max_value: 10.0
    step: 0.01
    mode: box 
    entity_category: config
    restore_value: true
    optimistic: true
    unit_of_measurement: ""
    icon: mdi:alpha-f
    web_server:
      sorting_group_id: sorting_equitherm


  # Max values
  - platform: template
    name: "CH Maximum Temperature"
    id: max_boiler_temp_heating
    min_value: 30
    max_value: 90
    mode: box 
    step: 1
    device_class: temperature
    restore_value: true
    optimistic: true
    icon: mdi:thermometer-high
    web_server:
      sorting_group_id: sorting_ch

  - platform: template
    name: "DHW Maximum Temperature"
    id: max_boiler_temp_water
    min_value: 30
    max_value: 90
    mode: box 
    step: 1
    device_class: temperature
    restore_value: true
    optimistic: true
    icon: mdi:thermometer-high
    web_server:
      sorting_group_id: sorting_dhw

  # Climate entity
  - platform: template
    name: "Preset away Temperature"
    id: away_temp
    device_id: ch
    icon: mdi:home-export-outline
    internal: true
    min_value: 5
    max_value: 20
    mode: box 
    entity_category: config
    step: 0.5
    initial_value: 12
    device_class: temperature
    restore_value: true
    optimistic: true
    web_server:
      sorting_group_id: sorting_ch


# Temperature compensation
  - platform: template
    id: indoor_temp_offset
    name: "Temperature Offset"
    icon: mdi:tune-variant
    entity_category: config
    device_id: ch
    device_class: temperature_delta
    restore_value: true
    mode: box  
    min_value: -5.0
    max_value: 5.0
    step: 0.1
    optimistic: true
    initial_value: 0.0
    disabled_by_default: true
    web_server:
      sorting_group_id: sorting_ch