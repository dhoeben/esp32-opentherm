  custom_opentherm:
    id:             otgw
    in_pin:         !secret pin_ot_rx
    out_pin:        !secret pin_ot_tx
    poll_interval:  !secret poll_interval
    rx_timeout:     !secret rx_timeout
    debug:          true

    boiler_temp:    boiler_temp
    return_temp:    return_temp
    modulation:     modulation
    setpoint:       setpoint