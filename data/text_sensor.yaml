text_sensor:
  - platform: template
    id: fault_text
    name: "Boiler Fault Detail"
    icon: mdi:alert-box-outline
    entity_category: diagnostic
    web_server:
      sorting_group_id: sorting_diagnostic

  - platform: wifi_info
    ip_address:
      name: WiFi IP Address
      entity_category: diagnostic
      web_server:
        sorting_group_id: sorting_diagnostic

    ssid:
      name: WiFi SSID
      entity_category: diagnostic
      web_server:
        sorting_group_id: sorting_diagnostic

    mac_address:
      name: WiFi MAC Address
      entity_category: diagnostic
      web_server:
        sorting_group_id: sorting_diagnostic