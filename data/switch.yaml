switch:
  # Emergency mode
  - platform: template
    id: emergency_mode
    name: "Emergency Mode"
    optimistic: true

  - platform: template
    id: force_heat
    name: "Force Heating"
    optimistic: true

  - platform: template
    id: force_dhw
    name: "Force DHW"
    optimistic: true